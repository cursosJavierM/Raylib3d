#pragma once
#include "raylib.h"

// Cabecera

class ZonaSegura
{
private:
    // Atributos
    Vector3 posicion;
    float size;

public:
    ZonaSegura(Vector3 nuevaPosicion, float NuevoSize);
    // Metodos
};
